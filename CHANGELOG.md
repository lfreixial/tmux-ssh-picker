# Changelog

## Unreleased

- Initial tmux SSH picker plugin.
- Discover hosts from SSH config, optional extra host files, and unhashed known_hosts.
- Add new saved SSH connections from inside the picker with `ctrl-n`.
- Support current-pane, new-window, horizontal-split, and vertical-split connection actions.
